import {createTheme} from "@mui/material/styles";

const GreenTheme = createTheme({
    palette: {
        default:"light",
        primary:{
            main: "#E4D00A"
            },
        secondary:{
            main: "#5F9EA0"
            }
        }
    })

export default GreenTheme
